export class Label {
    static readonly airport = "airport";
    static readonly country = "country";
    static readonly continent = "continent";
    static readonly route = "route";
    static readonly version = "version";
    static readonly supported = [
        Label.airport, Label.country, Label.continent, Label.route, Label.version]
}
